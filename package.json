{
  "name": "map",
  "version": "0.0.0",
  "scripts": {
    "start": "webpack serve --host 0.0.0.0 --progress --compress --mode=development",
    "build": "rm -rf dist; NODE_ENV=production webpack --mode production"
  },
  "dependencies": {
    "mapbox-gl": "^1.8.0"
  },
  "devDependencies": {
    "css-loader": "^3.0.0",
    "file-loader": "^4.0.0",
    "style-loader": "^0.23.1",
    "webpack": "^5.40.0",
    "webpack-cli": "^4.7.2",
    "webpack-dev-server": "^3.11.2"
  }
}
